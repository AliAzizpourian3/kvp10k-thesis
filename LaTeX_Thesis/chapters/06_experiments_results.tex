\chapter{Experiments and Results}
\label{ch:exresults}

% ---------------------------------------------------------------------------
\section{Overview: The Story of This Research}
\label{sec:story-overview}
% ---------------------------------------------------------------------------

This chapter presents the experiments and results of this thesis as a coherent
narrative. Each experiment was motivated by the findings of the previous one,
forming a logical chain from a simple generative baseline to a fully corrected
discriminative model that matches the published KVP10k benchmark.

The journey proceeds as follows: we begin with a generative baseline
(Mistral-7B) to establish a competitive reference point and understand the
failure modes of text-only generation on spatial documents. The discovery of
catastrophic hallucination on sparse layouts motivates a shift to a
layout-aware discriminative model. We then investigate two linking
granularities, discover a structural imbalance problem, and through systematic
diagnostic experiments identify and fix three compounding data-pipeline bugs --
arriving at a final corrected model with link F1 = 0.673, on par with the
published KVP10k baseline.

\subsection*{Evaluation Setup}

All experiments use the same evaluation set: \textbf{581 unique test pages}
from the KVP10k test split (55\% of 1{,}051 unique pages; limited by PDF
download failures and scanned pages without text layers). The evaluation
protocol follows Naparstek et al.\ (2024) exactly:
\begin{itemize}[nosep]
  \item \textbf{NED $< 0.2$} for text accuracy
  \item \textbf{IoU $> 0.3$} for location accuracy, using the same threshold as the KVP10k paper, which chose this value to permit minor spatial imprecision while still requiring the prediction to be in the correct spatial region of the page
  \item \textbf{F1} (primary metric) combining precision and recall
\end{itemize}

% ---------------------------------------------------------------------------
\section{Experiment 1: Generative Baseline (Mistral-7B QLoRA)}
\label{sec:exp1}
% ---------------------------------------------------------------------------

\subsection*{Setup}

The starting point of the experimental work is to establish a competitive
generative baseline that reproduces the approach used in the KVP10k paper.
Mistral-7B-Instruct-v0.2 is fine-tuned with 4-bit QLoRA (NF4 quantisation,
LoRA rank $r=4$, $\alpha=4$) on LMDX-formatted document prompts, where each
word in the document is represented as \texttt{word x1|y1|x2|y2} in
normalised coordinates. The model generates KVP predictions as structured text
output and is trained for 8 epochs with learning rate $5 \times 10^{-4}$.

\subsection*{Result 1: Competitive Performance, but Spatial Hallucination on Sparse Layouts}
\label{sec:res1}

\begin{table}[ht]
  \centering
  \caption{Experiment~1: Mistral-7B QLoRA results vs.\ KVP10k paper baseline ($n = 581$ test pages).}
  \label{tab:exp1-overall}
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    \textbf{System} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{TP / Pred / GT} \\
    \midrule
    \multicolumn{5}{l}{\textit{Text-only (NED $< 0.2$)}} \\
    KVP10k paper baseline & --- & --- & 0.643 & --- \\
    Mistral-7B (ours)     & 0.604 & 0.755 & \textbf{0.671} & 9{,}482 / 15{,}707 / 12{,}552 \\
    \midrule
    \multicolumn{5}{l}{\textit{Text + Location (NED $< 0.2$, IoU $> 0.3$)}} \\
    KVP10k paper baseline & --- & --- & 0.612 & --- \\
    Mistral-7B (ours)     & 0.539 & 0.675 & \textbf{0.600} & 8{,}473 / 15{,}707 / 12{,}552 \\
    \bottomrule
  \end{tabular}
\end{table}

Our Mistral baseline achieves text-only F1 = 0.671, slightly above the
published KVP10k baseline (0.643). This confirms our data preparation pipeline
and evaluation protocol are correctly implemented. The text+location F1 is
lower (0.600 vs.\ 0.612), primarily because a generative model outputs
approximate bounding boxes rather than exact pixel-level coordinates.

\paragraph{Per-cluster breakdown: the hallucination problem.}
\label{sec:stage3-per-cluster}
Broken down by layout cluster, however, a serious weakness emerges:

\begin{table}[ht]
  \centering
  \caption{Experiment~1 error analysis by layout cluster (entity-level,
  NED $<0.2$ and IoU $>0.3$, the same matching as Table~\ref{tab:exp1-overall}).}
  \label{tab:exp1-cluster}
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    \textbf{Cluster} & \textbf{Docs} & \textbf{Correct (\%)} & \textbf{Hallucination (\%)} & \textbf{Missed (\%)} \\
    \midrule
    0 (Dense)  & 521 & 58.6 & 41.4 & 32.5 \\
    1 (Sparse) &  60 &  3.0 & \textbf{97.0} & 38.1 \\
    \bottomrule
  \end{tabular}
\end{table}

On sparse-layout documents (receipts, simple forms), the model hallucinates
almost continuously: \textbf{97\%} of its predictions on sparse pages have no
correspondence in the actual document. Layout clusters here are the
\emph{actual} Stage~2
assignments, obtained by recomputing each test page's 13 layout features and
applying the trained Stage~2 scaler and $k$-means model. With very few KVPs
per page in sparse documents, the model over-generates (1{,}301 predicted
entities vs.\ 63 ground-truth for Cluster~1), producing a $20.6\times$
over-generation ratio. This is a
fundamental limitation of text-only generation without spatial grounding.

\textbf{Motivation for Experiment 2:} A model that reads spatial positions
directly -- rather than generating approximate coordinates -- should be able
to predict only tokens that actually exist in the document, eliminating
hallucination by design.

% ---------------------------------------------------------------------------
\section{Experiment 2: Layout-Aware Entity Classification (Stage 4a)}
\label{sec:exp2}
% ---------------------------------------------------------------------------

\subsection*{Setup}

We introduce LayoutLMv3-base (125M parameters) as the document encoder.
LayoutLMv3 jointly models text, layout (bounding boxes), and image patches,
giving it native spatial grounding. In Stage~4a, only the entity classification
head is trained (linker disabled): for each token, the model predicts whether
it is a Key, Value, or Other token. This phase establishes strong entity
representations before the more complex linking task is introduced.

\subsection*{Result 2: Strong Entity Classification}
\label{sec:res2}

\begin{table}[ht]
  \centering
  \caption{Experiment~2: Stage~4a entity classification results (best epoch).}
  \label{tab:exp2-results}
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    \textbf{Model} & \textbf{Precision} & \textbf{Recall} & \textbf{Entity F1} \\
    \midrule
    LayoutLMv3-base (entity only) & --- & --- & \textbf{0.847} \\
    \bottomrule
  \end{tabular}
\end{table}

Entity F1 = 0.847 confirms that LayoutLMv3 effectively classifies tokens as
keys, values, or other. Unlike Mistral, the discriminative model can only
predict labels for tokens that actually appear in the document -- there is
no mechanism for hallucinating text that does not exist. This establishes
strong entity representations as a foundation for the linking stage.

\textbf{Motivation for Experiment 3:} With good entity classification in place,
the next challenge is linking: pairing each predicted key span with its
corresponding value span.

% ---------------------------------------------------------------------------
\section{Experiment 3: Token-Level Linking (V1)}
\label{sec:exp3}
% ---------------------------------------------------------------------------

\subsection*{Setup}

We activate the biaffine linker, trained jointly with the entity classifier.
In V1, linking operates at the \textbf{token level}: each key token is scored
against each value token, producing a dense adjacency matrix of size
$N_{key} \times N_{value}$. On a typical page with $\sim$100 key tokens and
$\sim$100 value tokens, this produces $\sim$10{,}000 candidate pairs with
only $\sim$20 true positives ($\sim$450:1 neg:pos ratio). A
\texttt{pos\_weight} of up to 50 is applied to the binary cross-entropy loss
to counteract the imbalance. A $\lambda$ sweep over $\{0.5, 1.0, 2.0\}$
controls the relative weight of the linking objective.

\subsection*{Result 3: Token-Level Imbalance Kills Linking}
\label{sec:res3}

\begin{table}[ht]
  \centering
  \caption{Experiment~3: V1 token-level linking results ($\lambda$ sweep).}
  \label{tab:exp3-results}
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    \textbf{$\lambda$} & \textbf{Entity F1} & \textbf{Link F1} & \textbf{TP / Pred / GT} \\
    \midrule
    0.5 & 0.849 & 0.007 & --- \\
    1.0 & 0.845 & 0.008 & 15 / 200 / 3{,}776 \\
    2.0 & 0.848 & 0.006 & --- \\
    \bottomrule
  \end{tabular}
\end{table}

Link F1 remains near zero across all $\lambda$ values. The model predicts only
200 links against 3{,}776 ground-truth links, of which only 15 are correct.
The $\lambda$ sweep confirms this is not a hyperparameter issue: the failure
is \textbf{structural}. With a $\sim$450:1 neg:pos ratio, even aggressive
class-weighting cannot force the linker to make positive predictions reliably.

\textbf{Motivation for Experiment 4:} The problem is the \emph{granularity}
of the linking candidates. Linking should not happen at the token level but
at the span level -- grouping contiguous key/value tokens into spans before
scoring would reduce the candidate space from $\sim$10{,}000 to $\sim$225
entries and the imbalance from 450:1 to 5:1.

% ---------------------------------------------------------------------------
\section{Experiment 4: Span-Level Linking (V2)}
\label{sec:exp4}
% ---------------------------------------------------------------------------

\subsection*{Setup}

In V2, contiguous same-label tokens are grouped into spans, and the linker
scores key spans against value spans. This reformulation reduces:
\begin{itemize}[nosep]
  \item Candidates per page: $\sim$10{,}000 $\to$ $\sim$225
  \item Neg:Pos ratio: $\sim$450:1 $\to$ $\sim$5:1
\end{itemize}
V2 warm-starts from the best Stage~4a checkpoint with teacher forcing enabled
(ground-truth entity labels passed to the linker during training).

\subsection*{Result 4: Imbalance Resolved, But Near-Zero Link F1 Persists}
\label{sec:res4}

\begin{table}[ht]
  \centering
  \caption{Experiment~4: V2 span-level linking results.}
  \label{tab:exp4-results}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    \textbf{Model} & \textbf{Entity F1} & \textbf{Link F1} & \textbf{TP / Pred / GT} \\
    \midrule
    V2 (span-level) & 0.847 & 0.007 & 37 / 498 / 3{,}776 \\
    \bottomrule
  \end{tabular}
\end{table}

Despite the favourable class balance, link F1 is still near zero. The
granularity fix was necessary but not sufficient. This unexpected result
triggered a systematic diagnostic investigation to find the root cause.

% ---------------------------------------------------------------------------
\section{Experiments 5--7: Diagnostic Analysis (D1, D2, D3)}
\label{sec:exp567}
% ---------------------------------------------------------------------------

\subsection*{Setup}

Three targeted diagnostic experiments were run to identify why V2 failed
despite correct class balance:

\paragraph{D1 --- Link score distribution.}
Raw sigmoid scores extracted from the best V2 checkpoint on 200 test
documents. Goal: determine whether the linker produces any high-confidence
predictions or is uniformly near-zero.

\paragraph{D2 --- Entity span analysis.}
Comparing ground-truth vs.\ predicted entity token counts before and after
bounding-box validity filtering in the span grouping function, per document.

\paragraph{D3 --- Link label audit.}
Tracing the full link label generation pipeline from ground-truth KVPs through
word-level labels, token-level alignment, and span-level collapse.

\subsection*{Result 5--7: Three Compounding Bugs Identified}
\label{sec:res567}

\paragraph{Bug 1 --- Missing Teacher Forcing.}
The V2 linker received \texttt{argmax} predicted entity labels (noisy) instead
of ground-truth labels for span grouping. Span boundaries were corrupted by
classification errors, misaligning the linker's input with the link labels.
\emph{Fix: pass ground-truth entity labels to the linker during training.}
Outcome: entity F1 = 0.847, link F1 = 0.007 --- unchanged, indicating deeper issues.

\paragraph{Bug 2 --- Bounding Box Filter (D2).}
The \texttt{group\_contiguous\_spans} function applied the strict predicate
$(x_1 < x_2) \wedge (y_1 < y_2)$, but LayoutLMv3's normalisation to the
$[0, 1000]$ coordinate grid produces many zero-width tokens ($x_1 = x_2$).
The filter rejected \textbf{49\% of key tokens} and \textbf{46\% of value
tokens}, cutting entity spans roughly in half. Only padding tokens
($[0,0,0,0]$) should be excluded.
\emph{Fix: filter only $[0,0,0,0]$ bounding boxes (CLS, SEP, PAD tokens).}

\paragraph{Bug 3 --- Link Label Cross-Product Explosion (D3).}
The label function \texttt{\_generate\_labels} created a link label for
\emph{every} key-word index $\times$ \emph{every} value-word index within a
KVP. For a typical page with 8 KVPs, this produced $\sim$1{,}049 positive
entries instead of 8. After span-level collapse, nearly every key--value pair
was marked positive, providing no discrimination for the linker to learn from
(equivalent to a uniform-positive supervision signal).
\emph{Fix: generate exactly one positive link label per ground-truth KVP pair.}

\begin{table}[ht]
  \centering
  \caption{Link label pipeline before and after V4 corrections (20 test docs, 179 GT KVPs).}
  \label{tab:diagnostic-verification}
  \begin{tabular}{@{}lcc@{}}
    \toprule
    \textbf{Metric} & \textbf{Before (V2)} & \textbf{After (V4)} \\
    \midrule
    Words per document & $\sim$35 & $\sim$200 \\
    Token-level link positives (20 docs) & $\sim$20{,}000 & 1{,}013 \\
    Word-level link recovery & 100\%* & 79\% (141/179) \\
    Span-level link recovery & 100\%* & 70\% (125/179) \\
    \bottomrule
  \end{tabular}
  \smallskip
  \footnotesize{*Before V4, ``100\% recovery'' is misleading: the cross-product produced all possible positive links, not just correct ones. Effective supervision was a near-uniform positive matrix -- equivalent to no supervision.}
\end{table}

% ---------------------------------------------------------------------------
\section{Experiment 8: Corrected Model (V4)}
\label{sec:exp8}
% ---------------------------------------------------------------------------

\subsection*{Setup}

All three bugs are fixed and a corrected V4 model is trained from scratch with:
\begin{itemize}[nosep]
  \item Teacher forcing enabled
  \item Corrected bounding-box filter (only exclude $[0,0,0,0]$)
  \item Corrected link label generation (one positive per GT KVP pair)
  \item $\lambda = 5.0$ (increased to compensate for sparser but correct supervision)
  \item Early stopping patience = 10, maximum 30 epochs; best model at epoch 13
\end{itemize}

\subsection*{Result 8: Link F1 = 0.673 --- Matches KVP10k Baseline}
\label{sec:res8}

\begin{table}[ht]
  \centering
  \caption{Experiment~8: V4 corrected model results.}
  \label{tab:exp8-results}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    \textbf{Model} & \textbf{Entity F1} & \textbf{Link F1} \\
    \midrule
    V4 (corrected) & 0.827 & \textbf{0.673} \\
    KVP10k paper baseline (Naparstek et al., 2024) & --- & 0.659 \\
    \bottomrule
  \end{tabular}
\end{table}

With all three bugs corrected, the V4 model achieves \textbf{link F1 = 0.673},
surpassing the published KVP10k baseline (0.659). This result is achieved with
a 125M-parameter discriminative model trained on 55\% of the available
training data, directly demonstrating that the KVP10k paper's claim that
encoder-based models are insufficient for KVP linking was not empirically
founded.

% ---------------------------------------------------------------------------
\section{Summary and Narrative}
\label{sec:narrative-summary}
% ---------------------------------------------------------------------------

Table~\ref{tab:full-progression} summarises the full progression of
experiments.

\begin{table}[ht]
  \centering
  \caption{Full experimental progression.}
  \label{tab:full-progression}
  \small
  \begin{tabular}{@{}lccl@{}}
    \toprule
    \textbf{Model} & \textbf{Entity F1} & \textbf{Link F1} & \textbf{Key Finding} \\
    \midrule
    KVP10k paper baseline     & ---   & 0.659 & Published generative system \\
    Exp 1: Mistral-7B (text+loc) & --- & 0.600 & 97\% hallucination on sparse layouts \\
    Exp 2: Stage~4a (entity only) & 0.847 & --- & Strong entity classification \\
    Exp 3: V1 (token linking) & 0.841 & 0.008 & 450:1 imbalance kills linking \\
    Exp 4: V2 (span linking)  & 0.847 & 0.007 & Bugs mask span-level benefit \\
    Exp 5--7: Diagnostics     & ---   & ---   & 3 pipeline bugs identified \& fixed \\
    Exp 8: V4 (corrected)     & 0.827 & \textbf{0.673} & Matches and exceeds baseline \\
    \bottomrule
  \end{tabular}
\end{table}

The story of this research is one of progressive problem-solving. We began
with the question of whether a layout-aware discriminative model could match
a generative system that had not been benchmarked against encoder models.
We found that the generative approach, while competitive overall, fails
qualitatively on sparse documents due to hallucination -- a problem that
discriminative models avoid by construction. We then discovered that making
the discriminative model link key-value pairs is non-trivial: token-level
linking is defeated by class imbalance, and span-level linking only works
after three latent data-pipeline bugs are diagnosed and corrected.
The final corrected model demonstrates that a 125M-parameter discriminative
model, with the right supervision, can match a large generative system on
the KVP10k benchmark.
